{
  "name": "calculator",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "mathjs": "^3.20.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0",
    "react-tap-event-plugin": "^3.0.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && mkdir -p ../builds/kalculator && cp -R build/* ../builds/kalculator/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
